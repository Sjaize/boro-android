# Splash Screen Spec

이 문서는 `spec/screens/splash/images/`에 넣은 PNG를 기준으로, 화면 UI를 분해/정리한 결과가 채워지는 곳입니다.

## 1) 개요
- screen_id: splash
- 목적: (예: 앱 시작 시 브랜드/로딩 노출)

## 2) 사용자 시나리오
- 진입: 앱 실행
- 이탈: (예: 일정 시간 후) / (예: 초기화 완료 후)

## 3) 레이아웃(구성요소)
- (여기에 UI 컴포넌트/영역을 항목별로 정리)

## 4) 상태(State)
- default
- loading
- error(있다면)

## 5) 데이터/로직 계약
- 필요 입력(있다면):
- 타이밍/전환 조건:

## 6) 네비게이션
- 완료 후 이동: (예: `/home` 또는 `onboarding`)

## 7) 스타일 가이드
- 배경색/그라데이션/이미지: 
- 로고/문구 크기/위치(추정): 
- 애니메이션(있다면): 

## 8) 에러/빈화면 처리
- (없으면 '없음'이라고 적기)

## 9) 화면 근거(이미지 참조)
- 이미지 파일명: (예: `01.png`, `02.png`...)
- 영역 주석(있으면):
