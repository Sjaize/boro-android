급할 때 바로, 필요할 때 보로 글씨는 상단에서 330, 왼쪽에서 107, 오른쪽 101
텍스트 색상 : ffffff
Text(
  text = "급할 때 바로, 필요할 때 보로",
  style = TextStyle(
    fontSize = 16.sp,
    lineHeight = 5.sp,
    fontFamily = FontFamily(Font(R.font.pretendard)),
    fontWeight = FontWeight(500),
    color = Color(0xFFFFFFFF),
  )
)


boro
357, 77, 71, 401

Text(
  text = "Boro",
  style = TextStyle(
    fontSize = 48.sp,
    lineHeight = 5.sp,
    fontFamily = FontFamily(Font(R.font.ds_stereo)),
    fontWeight = FontWeight(800),
    color = Color(0xFFFFFFFF),
    textAlign = TextAlign.Center,
  )
)

메인 배경색은 #1570EF (Flutter: 0xFF1570EF)

스플래쉬 화면이다. 